<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".MainActivity"
    android:background="@android:color/background_dark">

    <TextView android:id="@+id/click_here"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:keepScreenOn="true"
        android:textColor="@color/text_color"
        android:textStyle="bold"
        android:textSize="50sp"
        android:gravity="center"
        android:text="start"
        android:clickable="true"
        android:layout_centerVertical="true"
        android:onClick=""/>
    <TextView android:id="@+id/click_here2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:keepScreenOn="true"
        android:textColor="@color/text_color"
        android:textStyle="bold"
        android:textSize="50sp"
        android:gravity="center"
        android:text="how to use"
        android:clickable="true"
        android:layout_below="@+id/click_here"
        android:onClick=""/>
    <TextView android:id="@+id/click_here3"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:keepScreenOn="true"
        android:textColor="@color/text_color"
        android:textStyle="bold"
        android:textSize="50sp"
        android:gravity="center"
        android:text="settings"
        android:clickable="true"
        android:layout_below="@id/click_here2"
        android:onClick=""/>
    <TextView android:id="@+id/click_here4"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:keepScreenOn="true"
        android:textColor="@color/text_color"
        android:textStyle="bold"
        android:textSize="50sp"
        android:gravity="center"
        android:text="about"
        android:clickable="true"
        android:layout_below="@id/click_here3"
        android:onClick=""/>

</RelativeLayout>
